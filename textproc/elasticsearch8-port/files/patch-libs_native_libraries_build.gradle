--- libs/native/libraries/build.gradle.orig	2026-08-05 21:29:08 UTC
+++ libs/native/libraries/build.gradle
@@ -66,30 +66,56 @@ dependencies {
     transformSpec.getFrom().attribute(ArtifactTypeDefinition.ARTIFACT_TYPE_ATTRIBUTE, ArtifactTypeDefinition.ZIP_TYPE);
     transformSpec.getTo().attribute(ArtifactTypeDefinition.ARTIFACT_TYPE_ATTRIBUTE, ArtifactTypeDefinition.DIRECTORY_TYPE);
   });
-  libs "org.elasticsearch:zstd:${zstdVersion}:darwin-aarch64"
-  libs "org.elasticsearch:zstd:${zstdVersion}:darwin-x86-64"
-  libs "org.elasticsearch:zstd:${zstdVersion}:linux-aarch64"
-  libs "org.elasticsearch:zstd:${zstdVersion}:linux-x86-64"
-  libs "org.elasticsearch:zstd:${zstdVersion}:windows-x86-64"
-  libs "org.elasticsearch:vec:${vecVersion}@zip" // temporarily comment this out, if testing a locally built native lib
+  if (System.getenv("LOCAL_ZSTD_BINARY") == null) {
+    libs "org.elasticsearch:zstd:${zstdVersion}:darwin-aarch64"
+    libs "org.elasticsearch:zstd:${zstdVersion}:darwin-x86-64"
+    libs "org.elasticsearch:zstd:${zstdVersion}:linux-aarch64"
+    libs "org.elasticsearch:zstd:${zstdVersion}:linux-x86-64"
+    libs "org.elasticsearch:zstd:${zstdVersion}:windows-x86-64"
+  }
+  // libs "org.elasticsearch:vec:${vecVersion}@zip" // temporarily comment this out, if testing a locally built native lib
+  if (System.getenv("LOCAL_VEC_BINARY_OS") == null){
+    libs "org.elasticsearch:vec:${vecVersion}@zip"
+  }
 }
 
+def buildLibVec = tasks.register('buildLibVec', Exec) {
+  workingDir = file('../../simdvec/native')
+  commandLine './gradlew', 'buildSharedLibrary'
+  outputs.dir(file("../../simdvec/native/build/libs/vec/shared/amd64/"))
+}
+
 def extractLibs = tasks.register('extractLibs', Copy) {
-  into layout.buildDirectory.dir('platform')
-  from configurations.libs
-  // TODO: fix architecture in uploaded libs
-  filesMatching("*-x86-64/*") {
-    it.path = it.path.replace("x86-64", "x64")
-  }
-  filesMatching("win32*/*") {
-    it.path = it.path.replace("win32", "windows")
-  }
-  includeEmptyDirs = false
-  filePermissions {
-    unix("644")
-  }
-  dirPermissions {
-    unix("755")
+  if (System.getProperty("os.name", "").startsWith("FreeBSD")) {
+    into layout.buildDirectory.dir('platform')
+    from('/usr/local/lib') {
+      include 'libzstd.so'
+      into 'freebsd-x64'
+    }
+    from(buildLibVec) {
+      include 'libvec.so'
+      into 'freebsd-x64'
+    }
+    filePermissions {
+      unix("755")
+    }
+  } else {
+    into layout.buildDirectory.dir('platform')
+    from configurations.libs
+    // TODO: fix architecture in uploaded libs
+    filesMatching("*-x86-64/*") {
+      it.path = it.path.replace("x86-64", "x64")
+    }
+    filesMatching("win32*/*") {
+      it.path = it.path.replace("win32", "windows")
+    }
+    includeEmptyDirs = false
+    filePermissions {
+      unix("644")
+    }
+    dirPermissions {
+      unix("755")
+    }
   }
 }
 
